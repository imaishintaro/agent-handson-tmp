# context.md 

# 以下のフォルダ構成に従ってファイルを整理する

~/workspace/
│
├── 📁 projects/          # 開発プロジェクト　プロジェクト名ごとにフォルダを作り開発
│   ├── appname1/
        ├──　scr/ スクリプト保管
        ├──. log/ ログ保管
　　　　　
│   ├── appname2/
│   ├── api/
│   └── experiments/
│
├── 📁 daily/             # 日常利用
│   ├── notes/
│   ├── scripts/
│   └── chat-logs/
│
├── 📁 memory/            # 記憶・コンテキスト管理 ★追加
│   ├── profile.md        # 自分の情報・好み・作業スタイル
│   ├── ongoing.md        # 進行中のタスク・続きから始めたいもの
│   └── knowledge/        # よく使う知識・調べたことのまとめ
│
├── 📁 assets/            # メディアファイル ★追加
│   ├── images/           # 写真・画像
│   ├── screenshots/      # スクリーンショット
│   └── docs/             # PDF・資料など
│
├── 📁 tmp/               # 一時フォルダ ★追加
│   ├── sandbox/          # とりあえず試す場所
│   └── inbox/            # 整理前のファイル置き場
│
├── 📁 tools/  #AIが使うツールなどを保管
│   ├── tool1/
│   └── tooo2/
│
└── 📁 shared/
    ├── utils/
    └── docs/



#　記憶について
ユーザーからの会話で記憶がないことがあれば、workspace/memory　内のmemoryフォルダを参照する
定期的にworkspace/memoryに会話概要を記録する



#　ツール実行環境
  アーキテクチャ

  ┌────────────────┬─────────────────────────────────┐
  │       層       │              技術               │
  ├────────────────┼─────────────────────────────────┤
  │ フロントエンド │ Discord（discord.js v14）       │
  ├────────────────┼─────────────────────────────────┤
  │ AIバックエンド │ Vercel AI SDK (ai v6)           │
  ├────────────────┼─────────────────────────────────┤
  │ モデル接続     │ OpenRouter経由 or Anthropic直接 │
  ├────────────────┼─────────────────────────────────┤
  │ 言語           │ TypeScript → tsc でビルド       │
  └────────────────┴─────────────────────────────────┘
  │ ハード          │ Macmini local環境       │
  └────────────────┴─────────────────────────────────┘



# 使えるツール一覧
- Bash: workspace内でシェルコマンドを実行（タイムアウト30秒）
- Read: ファイル読み込み
- Write: ファイル書き込み（親ディレクトリ自動作成）
- Edit: ファイル内の文字列を置換して編集
- Glob: ファイルパターン検索
- Grep: テキスト検索（正規表現可）
- WebSearch: ウェブ検索（SearXNG経由、localhost:8080）

全ツールはworkspace内のみアクセス可能。WebSearchのみ外部通信あり。


# !コマンド（ダイレクト実行）
ユーザーがDiscordで `!ls` のように `!` で始まるメッセージを送ると、AIを介さず直接シェルコマンドとしてworkspace内で実行される。
- 実行結果はDiscordにEmbedで返信される
- 禁止コマンドは同じルールでブロックされる
- 実行後、AIのchatHistoryにコマンドと結果が通知されるので、次の会話で認識できる


# WebSearch（ウェブ検索）
- SearXNGをDockerで起動して使用（workspace/projects/searxng/）
- 起動コマンド: `cd workspace/projects/searxng && docker compose up -d`
- API: `http://localhost:8080/search?q=検索語&format=json`
- 技術的な質問や最新情報が必要な時に積極的に使うこと


# 禁止コマンド
以下、が禁止しているコマンド　以下のコマンド以外のコマンドはbashで使えるので、
ユーザーの要望に応じて積極的に実行する
                                                                                              
  ┌────────────────────────────┬────────────────────────────────────┐
  │          パターン          │                説明                │                                  
  ├────────────────────────────┼────────────────────────────────────┤
  │ \bsudo\b                   │ 権限昇格                           │                                  
  ├────────────────────────────┼────────────────────────────────────┤
  │ \bsu\b                     │ ユーザー切り替え                   │
  ├────────────────────────────┼────────────────────────────────────┤
  │ \bchmod\b                  │ パーミッション変更                 │
  ├────────────────────────────┼────────────────────────────────────┤
  │ \bchown\b                  │ オーナー変更                       │
  ├────────────────────────────┼────────────────────────────────────┤
  │ \brm\s+(-[^\s]*)?-rf?\s+\/ │ rm -rf / 等ルートへの削除          │
  ├────────────────────────────┼────────────────────────────────────┤
  │ \brm\s+(-[^\s]*)?-rf?\s+~  │ rm -rf ~ ホームディレクトリ削除    │
  ├────────────────────────────┼────────────────────────────────────┤
  │ \bshutdown\b               │ シャットダウン                     │
  ├────────────────────────────┼────────────────────────────────────┤
  │ \breboot\b                 │ 再起動                             │
  ├────────────────────────────┼────────────────────────────────────┤
  │ \bmkfs\b                   │ フォーマット                       │
  ├────────────────────────────┼────────────────────────────────────┤
  │ \bdd\b\s                   │ ディスク書き込み                   │
  ├────────────────────────────┼────────────────────────────────────┤
  │ >\s*\/(?!dev\/null)        │ ルート直下への書き込みリダイレクト │
  ├────────────────────────────┼────────────────────────────────────┤
  │ \bcurl\b.*|\s*\bbash\b     │ curl | bash パイプ実行             │
  ├────────────────────────────┼────────────────────────────────────┤
  │ \bwget\b.*|\s*\bbash\b     │ wget | bash パイプ実行             │
  └────────────────────────────┴────────────────────────────────────┘